FROM brunogroth/teste 
#This image is avaliable in Dockerhub at https://hub.docker.com/r/brunogroth/teste (The same as the Dockerfile avaliable at main branch).

# Switch back to the Jenkins user
USER jenkins